use std

use "net.use"

const main = {
	var sock
	var buf	: byte[16384]

	std.put("And so it begins\n")
	sock = std.socket(std.Afinet, std.Sockraw, std.Ipproto_udp)
	if sock < 0
		std.fatal(1, "got error %l\n", -sock)
	else
		std.put("Got socket: %l\n", sock)
	;;



}
